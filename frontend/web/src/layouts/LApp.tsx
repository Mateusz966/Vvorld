import { Route } from 'react-router-dom';
import React, { Suspense, lazy } from 'react';

const LApp: React.FC<{}> = () => {
  return (
    <div>
      <Suspense fallback={null}>
    
      </Suspense>
    </div>
  )
}

export default LApp
